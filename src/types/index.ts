export type Category = {
  strCategory: string;
};

export type Meal = {
  strMeal: string;
  strMealThumb: string;
  idMeal: string;
};

export type searchForm = {
  search: string;
};

export type MealDetail = {
  [key: string]: string;
};
